# alertmanager-smtp — Sealed Secret
# ============================================================
# PLACEHOLDER — must be replaced with a real Sealed Secret
# before deploying to k3s.
#
# How to create:
#   1. Ensure kubectl is configured for the target cluster.
#   2. Run:
#        scripts/seal-secret.sh alertmanager-smtp monitoring smtp_password <your-smtp-password>
#   3. Commit the generated .sealed.yaml (never commit plaintext!).
#
# See: ADR-012 (Secrets Management Strategy)
#      scripts/seal-secret.sh
# ============================================================
# TODO: Replace this file with the actual SealedSecret output from seal-secret.sh
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: alertmanager-smtp
  namespace: monitoring
  annotations:
    sealedsecrets.bitnami.com/managed: "true"
spec:
  encryptedData:
    smtp_password: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: alertmanager-smtp
      namespace: monitoring
    type: Opaque
