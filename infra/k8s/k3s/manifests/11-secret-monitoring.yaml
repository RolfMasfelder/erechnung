# Secret for monitoring namespace (Grafana admin password)
apiVersion: v1
kind: Secret
metadata:
  name: monitoring-secrets
  namespace: monitoring
type: Opaque
data:
  # Base64: "admin" — change in production!
  GRAFANA_ADMIN_PASSWORD: YWRtaW4=
