# PostgreSQL Service für Test-Namespace
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: erechnung-update-test
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
