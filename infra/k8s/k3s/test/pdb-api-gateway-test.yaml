# PDB API Gateway für Test-Namespace
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-gateway-pdb
  namespace: erechnung-update-test
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api-gateway
