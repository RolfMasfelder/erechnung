# Redis Service für Test-Namespace
apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: erechnung-update-test
spec:
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
