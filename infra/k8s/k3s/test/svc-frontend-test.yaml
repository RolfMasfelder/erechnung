# Frontend Service für Test-Namespace
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: erechnung-update-test
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
